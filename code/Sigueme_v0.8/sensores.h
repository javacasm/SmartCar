// V 0.8

#define LTR !digitalRead(RightLineSensor)//declaramos la constante LTR que equivaldrá al valor digital (HIGH o LOW) que el RightLineSensor lea
#define LTM !digitalRead(MediumLineSensor)//declaramos la constante LTM que equivaldrá al valor digital (HIGH o LOW) que el MediumLineSensor lea
#define LTL !digitalRead(LeftLineSensor)//declaramos la constante LTL que equivaldrá al valor digital (HIGH o LOW) que el LeftLineSensor lea
